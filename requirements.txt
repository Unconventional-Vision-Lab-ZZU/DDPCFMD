# Install the PyTorch build appropriate for your hardware.
# Model execution was checked locally with PyTorch 2.5.0.
torch>=2.4,<3.0
# Prepared training samples use the original NPY/PNG layout.
numpy>=1.24,<3.0
Pillow>=9.5,<13.0
